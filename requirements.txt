# Requirements for Agent Memory Project
# Core dependencies (none required for basic functionality)

# Optional dependencies for LLM integration
openai==1.12.0  # For real LLM API calls; install if using OpenAI

# Optional dependencies for future enhancements
numpy==1.26.1  # For potential embedding calculations in RAG
scikit-learn==1.3.2  # For potential similarity metrics in RAG
sentence-transformers==2.2.2  # For potential semantic similarity in RAG 